services:
  - type: web
    name: last30days-mcp
    runtime: python
    buildCommand: bash build.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MCP_AUTH_TOKEN
        sync: false